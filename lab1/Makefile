# Makefile for My Python Program

# Define the Python interpreter
PYTHON = python3

# Default target
all: run

# Target to run the Python program with arguments
run:
	$(PYTHON) main.py $(U) $(K)

# Target to clean up generated files
clean:
	rm -f $(OUTPUT_FILE)